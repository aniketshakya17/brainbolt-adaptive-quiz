🚀 Run Full Application (Single Command)
Make sure Docker is installed.

From project root:

docker compose up --build

Frontend: http://localhost:3001
Backend: http://localhost:3000
Postgres: 5432
Redis: 6379

The seed script runs automatically.